server:
  port: 8083
spring:
  application:
    name: inventory-service
  cloud:
    config:
      uri: ${CLOUD_CONFIG_ENDPOINT:http://localhost:9999/config/}

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}